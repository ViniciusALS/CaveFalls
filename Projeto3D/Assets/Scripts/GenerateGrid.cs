using UnityEngine;

public class GenerateGrid : MonoBehaviour
{
    Mesh mesh;

    void Start()
    {
        mesh = new Mesh();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
